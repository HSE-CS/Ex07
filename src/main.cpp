// Copyright 2020 valvarl


#include <iostream>
#include "Fraction.h"

int main() {
/*    Fraction a(1, 2);
    Fraction b(a);
    Fraction c;
    std::cout << c.getValue() << std::endl; // 0
    c = a + b;
    std::cout << c.getValue() << std::endl; // 1
    a = Fraction{22, 9} * Fraction{9, 4} - Fraction{56, 9} * Fraction{3, 8};
    std::cout << a.getValue() << std::endl; // 19/6
    b = (Fraction{5, 7} + Fraction{3, 14}) / Fraction{20, 7};
    std::cout << b.getValue() << std::endl; // 13/40
    c = Fraction{14, 15} / Fraction{12, 25};
    std::cout << c.getValue() << std::endl; // 35/18*/
}
